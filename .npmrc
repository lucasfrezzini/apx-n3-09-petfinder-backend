# Enable scripts for package installation
enable-pre-post-scripts=true

# Allow build scripts to run
ignore-workspace-root-check=true

# Specifically allow bcrypt to run its build scripts
public-hoist-pattern[]=bcrypt

# Use hoisted node_modules for better compatibility
node-linker=hoisted

# Enable unsafe permissions for native module builds
unsafe-perm=true

